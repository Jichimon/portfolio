# ADR index — mandatory entry point

**Read this before any ADR.** An ADR is almost never refuted wholesale; it is amended point by point. Consulting one without this index risks applying a decision a later point already overturned — which is a defect, not a style issue. Citing a refuted decision is itself a finding (`P-07`).

TASK 7 writes the founding set: site stack, hosting and deploy, i18n strategy, content pipeline, testing toolchain, publication. **6 of 6 accepted — TASK 7 closed 2026-08-19.**

`TASK 33` adds the seventh on 2026-08-23 — the UI component model — and amends the testing toolchain in the same change. `TASK 35` adds the eighth on 2026-08-24 — how the implementation tree is arranged. It was the first ADR to sit in this index **before** being accepted, so it was findable while it was still a proposal rather than only after; accepted the same day. **The level-2 table below stopped being empty on that date**, so the three-part discipline it describes is now load-bearing rather than theoretical.

`TASK 71` adds the ninth on 2026-08-28 — delegation economics — and it is **the first ADR about the harness rather than about the site**. It was listed here while `Proposed`, following `ADR-008`'s precedent, so it was findable before it was accepted — **accepted the same day**. It adds **no level-2 row**: it opens ground none of the eight covered, and cites `ADR-006` and `ADR-008` rather than reopening them.

**Its own eighth sub-decision was added the same day, and there is no level-2 row for that either — deliberately.** The level-2 table records amendments to **accepted** decisions, so that a reader who trusts a point learns it has moved. `ADR-009` was not yet accepted when §8 was written; no reader could have been relying on it, and the sub-decision it corrects is one of its own drafts. **From now on it is accepted, so the next change to any of its eight points does take a level-2 row.** Revising a proposal is not amending a decision, and recording it as one would make the level-2 table describe something other than what it claims to. The revision is recorded **inside** `ADR-009` instead, where its review-trigger note says which trigger fired and why it was replaced.

## Level 1 — status of each ADR

**States:** `Current` · `Current-with-amendments` (still standing, some points amended — see level 2) · `Superseded by ADR-<NNN>` (refuted in full).

| ADR | Title | Date | Status |
|---|---|---|---|
| [ADR-001](ADR-001-site-stack.md) | Site stack — Astro, static output | 2026-08-19 | `Current` |
| [ADR-002](ADR-002-content-pipeline.md) | Content pipeline — frontmatter validation and diagram resolution | 2026-08-19 · amended 2026-08-24 · extended 2026-08-27 | `Current-with-amendments` |
| [ADR-003](ADR-003-i18n-strategy.md) | i18n strategy — unprefixed English, `/es/` Spanish | 2026-08-19 · amended 2026-08-24 | `Current-with-amendments` |
| [ADR-004](ADR-004-hosting-deploy.md) | Hosting and deploy — Cloudflare Workers, static assets | 2026-08-19 | `Current` |
| [ADR-005](ADR-005-publication.md) | Publication — public GitHub remote, now, whole repository | 2026-08-19 | `Current` |
| [ADR-006](ADR-006-testing-toolchain.md) | Testing toolchain — `node:test`, Stryker (`tap-runner`), Playwright | 2026-08-19 · amended 2026-08-23, 2026-08-24 (×2), 2026-09-01 | `Current-with-amendments` |
| [ADR-007](ADR-007-ui-component-model.md) | UI component model — `.astro` by default, Preact for islands, zero islands today | 2026-08-23 | `Current` |
| [ADR-008](ADR-008-site-implementation-architecture.md) | Site implementation architecture — framework-free core, Astro gateway, content as the only source of copy, and how the code reads | 2026-08-24 · amended 2026-08-24, 2026-08-27 | `Current-with-amendments` |
| [ADR-009](ADR-009-delegation-economics.md) | Delegation economics — when not to delegate, what a brief carries, who assembles the context, and what a run costs | 2026-08-28 · §8 added the same day, pre-acceptance | `Current` |

## Level 2 — amendments, point by point

An amended point does **not** invalidate the whole ADR — only that point.

| Verb | Meaning | Marked inline in the ADR? |
|---|---|---|
| ↪️ **Extended** | Still valid; another document expands it | No — the point stands as written |
| ✏️ **Amended** | Changed in part; its old form no longer applies | Yes |
| ⛔ **Refuted** | No longer in effect (the rest of the ADR stands) | Yes |
| 🔁 **Superseded** | The entire ADR was replaced | Yes |

| ADR · § | What the point said | Verb | Superseded by | Date |
|---|---|---|---|---|
| [ADR-006](ADR-006-testing-toolchain.md) · Decision, Review trigger | Two tiers: `node:test` for content logic, Playwright for e2e. Vitest enters only if `site/lib/content/**` turns out to need Vite. Nothing decided about testing a component | ✏️ **Amended** | [ADR-006 § Amendment · 2026-08-23](ADR-006-testing-toolchain.md#amendment--2026-08-23--the-component-test-tier) — a third tier, Vitest + `@testing-library/preact` in `jsdom`, for DOM-requiring behaviour modules and Preact islands. The named trigger has **not** fired; its *policy* is what governs. Mutation coverage unchanged | 2026-08-23 |
| [ADR-008](ADR-008-site-implementation-architecture.md) · Sub-decision 6 | *"root — `start` and `test` only, and no dependencies"*, carried into `S-07` as a rule | ✏️ **Amended** | [ADR-008 § Amended 2026-08-24](ADR-008-site-implementation-architecture.md) — Stryker's sandbox is rooted at the working directory, so one config covering both mutation surfaces can only live at the root. The rule becomes *only tools whose configuration must live at the repository root to function*. **This row was missing until 2026-08-24**: the amendment was written inline and the index was never reconciled — `P-07`'s characteristic failure, found by the next session rather than by the one that caused it | 2026-08-24 |
| [ADR-001](ADR-001-site-stack.md) · Decision | *"whether any specific piece of the site's UI should be a React island, plain CSS/JS, or a View Transition — that is a design decision for TASK 8"* | ↪️ **Extended** | [ADR-007](ADR-007-ui-component-model.md) — the deferral is answered, not overturned. `TASK 8` closed without recording it; `TASK 33` did. No inline mark and no status change: the point deferred a decision rather than asserting something now false | 2026-08-23 |
| [ADR-003](ADR-003-i18n-strategy.md) · Decision | *"Astro's own auto-generated `id` (via `github-slugger` on the filename, e.g. `mobile-banking-platform-en`)"* | ⛔ **Refuted** | [ADR-003 § Amended 2026-08-24](ADR-003-i18n-strategy.md) — false for `astro@7.2.5`, and **in the dangerous direction**. The loader's default id is `data.slug` whenever frontmatter carries one, which every file here does, so both halves of a locale pair generate the same id and one silently overwrites the other. The advice *not* to pair on the id still stands; the reason given for it described behaviour that does not exist. Found by the first real build failing, not by review | 2026-08-24 |
| [ADR-003](ADR-003-i18n-strategy.md) · Explicitly not decided here | Two open items: the in-body link-rewriting mechanism, and whether `i18n.fallback` fires for collection-driven routes | ↪️ **Extended** | [ADR-003 § Amended 2026-08-24](ADR-003-i18n-strategy.md) — both answered, neither overturned. The rewrite is a pure function in the content core, with the markdown-pipeline wiring deliberately left to the item that renders prose. The fallback is **not relied on** rather than tested: routes are explicit and an orphaned locale throws at build. No empirical test was run, and that is stated rather than implied | 2026-08-24 |
| [ADR-008](ADR-008-site-implementation-architecture.md) · Sub-decision 5 | *"No folder under `site/**` holds seven or more files"*, one number for every directory, carried into `S-03` | ✏️ **Amended** | [ADR-008 § Amended 2026-08-27](ADR-008-site-implementation-architecture.md) — two calibrations. Ordinary directories keep 6; a **package root**, derived from disk as a directory holding `package.json`, gets 10. The scope was the defect, not the number: a package root is not a directory anyone organises, so the remedy the cap asks for — split by context — is unavailable to it. At eleven the answer is still not a higher number | 2026-08-27 |
| [ADR-002](ADR-002-content-pipeline.md) · both sub-decisions | Silent on caching: the pipeline was specified without saying what invalidates its output | ↪️ **Extended** | [ADR-002 § Extended 2026-08-27](ADR-002-content-pipeline.md) — the cache is keyed on the pipeline's own inputs (core, config, lockfile), not cleared. Neither sub-decision is overturned; the gap they left is closed. Measured: the same neutered plugin emitted the defect in 0 files warm and 10 cold, and an unchanged pipeline still builds warm at 2.56s | 2026-08-27 |
| [ADR-003](ADR-003-i18n-strategy.md) · Decision | *"Cross-locale links use `astro:i18n`'s `getRelativeLocaleUrl(locale, path)`"* | ✏️ **Amended** | [ADR-008](ADR-008-site-implementation-architecture.md) places locale URLs in the framework-free core and `S-06` makes it enforceable — a module importing `astro:i18n` leaves the reach of both `node:test` and the mutation gate. The later decision governs; no `astro:i18n` import exists | 2026-08-24 |
| [ADR-006](ADR-006-testing-toolchain.md) · every glob naming the surface | The unit-and-mutation surface spelled `site/lib/content/**` — written when that was the whole of the core as anyone had imagined it, before `ADR-008` or a single file under `site/` existed | ✏️ **Amended** | [ADR-006 § Amendment · 2026-08-24 — the surface was named as one directory](ADR-006-testing-toolchain.md#amendment--2026-08-24--the-surface-was-named-as-one-directory-and-the-core-is-not-one-directory) — all four globs widened to `site/lib/**`, matching `S-06`, which already scoped the whole core. **Proven in red:** a failing test planted in a sibling directory left the old glob exiting **0**. Also closes this ADR's first review trigger **in the negative** — the core exists, runs under plain `node --test`, and does not need Vite | 2026-08-24 |
| [ADR-002](ADR-002-content-pipeline.md) · Sub-decision 1, Decision | Option C, the minimal schema, spelled with Zod's `.passthrough()` for the non-universal keys | ✏️ **Amended** | [ADR-002 § Note · 2026-08-24](ADR-002-content-pipeline.md#-note-2026-08-24--the-method-was-renamed-the-decision-was-not) — **the decision is untouched; only the method name is.** Written against Zod 3 docs before anything was installed; `astro@7.2.5` resolves `zod@4.4.3`, where `.passthrough()` is deprecated in favour of `z.looseObject()`. Five universal keys, `byType` still owned solely by `check-content`. Read from `node_modules`, not from a release page (`C-01`) | 2026-08-24 |
| [ADR-006](ADR-006-testing-toolchain.md) · Decision (E2E runner) | Playwright's "three browser engines (Chromium, Firefox, WebKit)" read as what `e2e smoke` runs in CI, not only as a property of the tool | ✏️ **Amended** | [ADR-006 § Amendment · 2026-09-01](ADR-006-testing-toolchain.md#amendment--2026-09-01--e2e-narrowed-to-chromium-for-ci) — narrowed to Chromium only for the CI-blocking gate. First real evidence: a monitored CI run showed `e2e smoke` still in flight at 89 minutes (an orphan `astro preview` process), with `mutation` never reached — the opposite of what every prior local-only measurement assumed. Firefox/WebKit coverage is a stated, accepted gap (`C-11`), restorable in `playwright.config.ts` if a real cross-engine defect is ever found | 2026-09-01 |

## How to keep this alive

**Three parts, all load-bearing.** When amending or refuting a point: add the level-2 row (so the amendment is findable), adjust the level-1 status (so a reader knows to look), and add the **inline annotation at the exact paragraph** of the affected ADR (so someone reading it top to bottom cannot miss it).

The characteristic failure is doing the first and skipping the other two. That is why `P-07` requires re-reading after reconciling: *reconciling and checking you reconciled are different acts, and only the second produces evidence.*

At wrap-up, audit that no point refuted during the work was left unmarked.
