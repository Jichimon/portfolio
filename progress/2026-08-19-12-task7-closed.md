# 2026-08-19 · Session 12 — TASK 7 closed: 6/6 founding ADRs, the harness's first real acceptance test

**Task:** TASK 7 — Founding ADRs
**Status after this session:** `DONE`. 6/6 ADRs accepted. `TASK 8` and `TASK 15` unblocked.

## What was done

Across this whole session (progress logs `2026-08-19-01` through `2026-08-19-11` carry the per-decision detail), ran all six of TASK 7's decisions through `work-item` (minus the spec step — `research` type, the ADR is the artifact), one at a time, each with its own researcher pass and its own human checkpoint:

1. **Site stack** → `ADR-001`: Astro, static output.
2. **Content pipeline** → `ADR-002`: minimal Zod schema, one-time diagram pre-render (`TASK 17`), zero Mermaid at build time.
3. **i18n strategy** → `ADR-003`: unprefixed English, `/es/` Spanish, routes joined on `slug`.
4. **Hosting and deploy** → `ADR-004`: Cloudflare Workers, static assets.
5. **Publication** → `ADR-005`: public GitHub remote, now, whole repository.
6. **Testing toolchain** → `ADR-006`: `node:test` + Stryker's `tap-runner`, Playwright.

Along the way, two content items (`TASK 16`, `TASK 17`) and one harness bugfix (`TASK 18`, from `INC-15`) were opened and mostly closed as side effects of decisions 2 and 4. `TASK 13` (Capture K1) was done first, deliberately, so this run would produce a real number instead of another anecdote.

## K1 — the number this run exists to produce

Per-decision, human-visible implement→verify cycles (`TASK 13`'s definition — a checkpoint round or a rejected/revised artifact sent back, never a tool call):

| Decision | Passes | What the second pass was, where there was one |
|---|---|---|
| 1. Site stack | 2 | Author asked whether Astro forces plain-HTML vs. React — a real question the first draft hadn't addressed, not a defect |
| 2. Content pipeline | 2 | Author questioned the ongoing Mermaid/Puppeteer build dependency — led to a better design (one-time pre-render, `TASK 17`), not a correction of an error |
| 3. i18n strategy | 1 | Approved as drafted |
| 4. Hosting and deploy | 1 | Approved as drafted (interrupted by `INC-15`'s trace incident, unrelated to the ADR's own content) |
| 5. Publication | 1 | Author's preference gathered before drafting (`AskUserQuestion`), so the draft matched on the first pass |
| 6. Testing toolchain | 2 | Author pushed back on the Vitest-vs-`node:test` reasoning — second pass **found and corrected a real factual error** in the first draft (a false claim that no dedicated Stryker runner exists for `node:test`) |

**Max 2, never exceeded. Sum 9 across 6 decisions.** `TASK 7`'s own acceptance bar — "if it needs more than two implement→verify passes, K1 says so and the harness gets corrected before any site code is written" — read per decision (the natural unit here, each with its own checkpoint), the harness passed. More importantly than the number: neither of the two 2-pass decisions was the checkpoint catching a *harness* failure — one was the author extending scope with a legitimate question, the other was the checkpoint catching this session's own reasoning error before it shipped. That is the checkpoint discipline working as designed, not overhead.

## Decisions (session-level, not already logged per-ADR)

- **`TASK 7`'s Done condition "the gate's sub-gates real" is interpreted as "documented with exact commands," not "wired into `gate.mjs`."** `site/` does not exist yet — there is nothing for a unit/mutation/e2e gate step to run against. `ADR-006` names the exact commands; wiring them is `TASK 15`'s and `TASK 8`'s job. Stated explicitly here rather than silently assumed, since `P-03` makes silence read as coverage.
- **`TASK 8`'s scope was amended mid-session** (not researched, the author's own idea): it must generate a design/UX task and an `INC-03` visual-QA item (comparing the real site, local and deployed, against the design) — `docs/harness/architecture.md` §M already named `TASK 8`'s trigger as exactly this moment.

## Findings from validating against real state (P-04)

- The one real harness defect found this session (`INC-15` — trace redaction flagging a coincidental substring in an opaque `tool_use_id`) was found by an actual `check-trace` run failing, not constructed for the eval suite — `EC-014` records it live.
- A researcher role hit its declared `maxTurns` budget without delivering a report, twice, independently (decisions 2 and 4's research). Both times a `SendMessage` resume produced the full report on the next turn. Not diagnosed further this session — filed as a pattern worth `harness-evaluator`'s attention, since `maxTurns: 25` is declared but the actual tool-call counts observed (44, 48) were well past it.

## Done

```yaml
done:
  docs:       { status: passed, evidence: ["docs/adr/README.md: 6/6", ".claude/rules/30-testing.md: all five rows filled", "TASKS.md: TASK 7 DONE, TASK 8 and TASK 15 unblocked"] }
  gate:       { status: passed, evidence: ["node scripts/gate.mjs", "exit:0, 13 steps green"] }
  scope:      { status: passed, evidence: ["6/6 decisions resolved, none deferred, none speculative — every blank row filled from an actual decision or left explicitly open with a named trigger"] }
  iterations: { status: passed, evidence: ["9", "6 decisions, per-decision: 2, 2, 1, 1, 1, 2 — max 2, sum 9, detailed above"] }
```

## Open questions

- Two recurring researcher-budget overruns (decisions 2 and 4) — not root-caused this session, flagged for `harness-evaluator`.
- `TASK 18` (trace redaction false-positive fix) — opened, not yet implemented.
- `TASK 16` (about-page aside) and `TASK 17` (diagram pre-render) — both `DONE`, applied by the author directly per `H-02`.

## Next

`TASK 8` — Site work breakdown, now unblocked. Its constraints already name the design/UX and visual-QA items it must include.

## Files changed

`TASKS.md` — `TASK 7` → `DONE`, `TASK 8` and `TASK 15` unblocked.
`docs/adr/README.md` — 6/6, `ADR-006` row.
`.claude/rules/30-testing.md` — all five stack-dependent rows filled.
`progress/2026-08-19-12-task7-closed.md` — this file, new.
