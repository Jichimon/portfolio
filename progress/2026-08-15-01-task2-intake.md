# 2026-08-15 · Session 01 — TASK 2 intake questionnaire

**Task:** TASK 2 — Site copy
**Status after this session:** IN PROGRESS

## What was done

Researched `TASKS.md`, the latest prior log, `private/glossary.md`,
`private/banned-terms.txt`, all 10 case studies, and the author's CV to scope
TASK 2 (confirmed: 8 files — `home`/`about`/`experience`/`contact` × en/es)
without re-asking anything already resolved elsewhere. Drafted
`resources/site/intake.md` (Spanish, Critical/Important/Optional groups,
`[BLOCKING: file]` tags) after the author corrected the initial
employer-anonymization recommendation and flagged leaving NICE on July 1st.
Verified the final file against `./scripts/check-terms.sh`, which caught and
fixed one real leak before this log was written.

## Decisions

- **Name real employers on `home`/`about`/`experience`/`contact`; case studies
  and diagrams stay generic, unchanged.** Why: a generic descriptor ("a
  regulated bank") gives no traceability and can't carry a logo, which the
  author wants to show. Rejected: my original recommendation to anonymize
  everywhere to match the case-study convention — the author corrected this
  directly; that convention protects system internals, not employer identity,
  and I had over-extended it.
- **Did not edit `private/banned-terms.txt` / `private/glossary.md` this
  session**, even though naming employers will require it (see Next).
  Rejected: editing now — belongs to the session that actually drafts
  `about.md`/`experience.md`, not the one writing the questionnaire.
- **`resources/site/intake.md` still uses period+role descriptors instead of
  real names for 3 of the 4 employers (all but NICE).** Confirmed by actually
  running `check-terms.sh` against the drafted file — it failed on `"Banco
  Sol"` as a substring of the bank's full legal name on the first pass.
  Rejected: trusting an earlier inference (from a sub-agent's summary of the
  glossary) that the full legal name was a safe string to write — it wasn't;
  fixed by descriptor + rerun rather than assuming.
- **Added `resources/site/intake.md` to `.gitignore`, one file, not a
  pattern.** Why: answers will cover availability, active job search, and the
  employment relationship of two roles — working notes, not for the repo
  behind a public site. Rejected: a broader `resources/site/*` pattern — risks
  hiding the real deliverables once `home.md` etc. exist.
- **Narrowed the GitHub question to concrete repo content, and moved "should
  this content repo itself go public" out of the questionnaire.** Why: naming
  employers on the site already makes the original "this repo could
  de-anonymize the bank" framing moot; the public-repo question is bigger and
  the author wants to think it through separately.
- **Did not correct `CLAUDE.md`'s "currently at NICE" line this session.**
  Why: it's stale (the author left NICE July 1st), but the author chose to
  specify their current status inside the intake itself rather than resolve
  it live in chat. Rejected: guessing at replacement wording now — risk of
  stating something inaccurate in a binding project file.

## Open questions

- Whether to make this content repo itself public is unresolved. The author
  flagged it needs separate analysis — it affects how (or whether) GitHub gets
  linked from `contact.md` (see intake C6). Not forced into the questionnaire.

## Next

Wait for the author to complete `resources/site/intake.md`. Once answered:
1. Correct `CLAUDE.md`'s stale "currently at NICE" line using the C2 answer.
2. Update `private/banned-terms.txt` / `private/glossary.md` to drop the
   entries blocking the bank's formal name and the two 2021–2023 employers'
   names, while leaving the bank's existing informal-short-name/app-name
   mapping row untouched (that one isn't about the employer's identity) —
   needed before `about.md`/`experience.md` can name them and still pass
   `check-terms.sh`.
3. Move to Etapa 2 (devolución) per the author's three-stage process for
   TASK 2 — not before.

## Files changed

`resources/site/intake.md` — new; the TASK 2 intake questionnaire (gitignored, not committed).
`.gitignore` — added `resources/site/intake.md` as a single-file ignore rule.
`TASKS.md` — TASK 2 status `TODO` → `IN PROGRESS` (intake sent — awaiting author's answers).
`progress/2026-08-15-01-task2-intake.md` — this log.
