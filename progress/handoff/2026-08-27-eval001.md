# Hand-off — `TASK 60`: run `EVAL-001`

**Written 2026-08-27, by the session that reconciled `TASKS.md` and built the trace index below is designed for.** This packet is everything a fresh session needs to run the evaluation without re-deriving context (`P-10`). Read it, then read the two artifacts it points at, then act.

## The goal, in one sentence

Produce `progress/evaluation-results/EVAL-001-<slug>.md`, in `EVAL-TEMPLATE.md`'s fixed shape, scoring the harness against the first real non-harness work — the six site items closed since `EVAL-000` — and turning every `Gap` into a tracked entry in `TASKS.md`.

## How to start

```
/evaluate-harness

The work item is TASK 60. The precomputed trace index is
progress/2026-08-27-12-eval001-trace-index.md — read it before opening
any file under evidence/runs/ directly.
```

`/evaluate-harness` has `disable-model-invocation: true`, so a human has to type this — it cannot be self-invoked mid-session.

## Why this is `TASK 60` and not a bare "run the evaluation"

`harness-evaluator` holds `Write`, which makes it write-capable under `H-05`'s `delegation-gate`. That guard denies any write-capable delegation whose brief names zero work items (`extractWorkItems` requires at least one `TASK-N` match in the brief text). Nothing in the register covered "run the harness evaluation" before this session — `TASK 60` is that id now, typed `harness`, so `specRequiredFor` does not apply and no spec is needed before delegating.

**The one thing that must not happen:** never let `TASK-22`, `TASK-23`, `TASK-24`, `TASK-25`, or `TASK-26` appear as a bare `TASK N` pattern anywhere in the delegation brief — not even quoted from this document, not even inside an error message you're relaying. All five are typed `feature`; `extractWorkItems` scans the *whole* brief text with `/\bTASK[\s-]?(\d+)\b/gi`, and naming one triggers `specRequiredFor`'s check for an approved spec. Their specs likely are approved (the items are `DONE`), but the backlog's own history records two real denials from exactly this pattern (see `TASKS.md`, "A second cost, found by hitting it twice" under the site implementation backlog). Cite them descriptively instead: *"the content-layer item"*, *"the tokens-and-layout item"*, *"the two page-template items"*, *"the about/experience/404 item"*. `TASK 60` itself is fine to cite — it is the id governing this delegation.

## Why now, not after `TASK 27`

`EVAL-000` closed with an explicit instruction: *"`EVAL-001` — which should be run over the first non-harness work item — is read as the first evaluation with any external validity."* Every completed work item until now was harness work on the harness's own author's workload — `EVAL-000` named that its own worst bias ("Composition — total"). The six site items (content layer, tokens/layout, home, case-study templates, about/experience/404, plus nine bugfix/harness items they opened) are the first non-harness workload this process has run. Running `TASK 27` first would spend that first real data point on more harness work and reproduce the exact bias `EVAL-000` flagged.

## What to read, in order

1. **`progress/2026-08-27-12-eval001-trace-index.md`** — every trace file's header/footer/posture/deny events, already extracted with exact pointers. This exists so the corpus (104 trace files, 35 run directories) doesn't have to be read exhaustively inside a 60-turn budget. It also names three anomalies found while building it — read those before scoring, they bear on `EVAL-000`'s open gaps (`GAP-06`, `GAP-07`, `GAP-08`) and on `L`.
2. `evaluation-cases/EC-TEMPLATE.yaml` and the fourteen cases beside it (per `harness-evaluator`'s own bootstrap).
3. `progress/evaluation-results/EVAL-TEMPLATE.md` — the scorecard shape.
4. `progress/evaluation-results/EVAL-000-baseline.md` — the prior scorecard. Its KPI table, its declared biases, and its open gaps (`GAP-05` through `GAP-09`) are the comparison baseline `EVAL-001`'s own template requires filling.
5. `docs/harness/evidence.md`, `docs/harness/architecture.md` §K, `docs/harness/contracts.md` §6 — already named in `harness-evaluator`'s own bootstrap; the role reads these itself.

## What's different from the baseline — so the numbers aren't a surprise

| | `EVAL-000` | Now |
|---|---|---|
| Sessions/runs scored | 2 | 35 run directories, 104 trace files |
| `run.header` events | — | 176 |
| `run.footer` events | none legible | 106 |
| `policy.decision` denies | 5 (3 false positives) | 40 |
| `permission_mode` observed (not `unknown`) | 0/118 | 9/176 (5 `auto`, 4 `plan`) |
| Completed non-harness work items | 0 | 6 |

The posture-observed number (9/176) is new since 2026-08-27's trace-fidelity slice landed — before it, every header on disk read `unknown`. No run in the corpus shows `bypassPermissions`.

## KPIs that now have a denominator for the first time

- **K1 passes-to-done** — was `unmeasurable`, raw 0, in the baseline. Six items are now `DONE`. The `iterations` dimension in each item's `progress/` log's `done:` block is `self-reported` — mark it as such, never presented beside an `observable` figure with equal confidence.
- **K3 escaped defects** — expect a real, nonzero count: nine bugfix/harness items were opened *by* the site work (glob coverage gaps, a self-staling pending-routes list, two e2e assertions that passed alone and failed under load, a screenshot step reporting PASS while writing no image, a gate reporting PASS with a type error on disk, a stale-cache defect, a substring-collision in the confidentiality guard, a silently-disabled write-time scrubber, an unreferenced photo that would have published). A high K3 here is the harness's early-warning system working, not a failure to hide.
- **L context load** — the trace index already answers this: **0 of 332 `instructions.loaded` events sit in any delegated trace**, across 70 delegated files. Unchanged from the baseline's finding on the delegated path. Report it as unmeasured for delegation, not as 100%.
- **V rule violations** — 40 denies total. Classify each as genuine or false the way `EVAL-000` did (it found 3 of 5 were the harness's own defect firing, not danger averted) — the trace index's deny table already has every pointer; this is where the evaluator's judgment does real work.

## Boundaries, restated because they're easy to forget mid-run

- Writes only inside `progress/evaluation-results/` — enforced by a guard, not by instruction.
- Never edits an artifact it's scoring — a spec, a guard, a rule, a work log. A wrong one is a finding, not something to fix in place.
- Never invokes a git write (`H-01`); never touches `resources/` or `evidence/` (`H-02`, `H-03`); never reads `private/` (`H-04`).
- Score the harness, not the model (`A16`): a pass is a guard verdict plus a trace shape, never "the model declined to do something dangerous."

## After it closes

Read `EVAL-001-<slug>.md`'s Gaps section and open one `TASKS.md` entry per gap, each with a done someone else could check (`P-06`) — that part is the orchestrator's, not the evaluator's; `harness-evaluator` cannot write to `TASKS.md`.
