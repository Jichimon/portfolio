# Hand-off — `TASK 64`: trace fidelity, second pass

**Written 2026-08-29, by the session that closed `TASK 61`.** This packet is a claim, not
ground truth — validate it against the real repository before acting on it (`P-04`, `P-11`).

## The goal, in one sentence

Start `TASK 64` next — it is the author's own stated order (`65 → 63 → 61 → 64 → 66 → 67 → 75`),
still `TODO`, and the four trace-writer defects it fixes are unrelated to anything this
session touched.

## How to start

```
/work-item TASK-64
```

`/work-item` carries `disable-model-invocation: true`, so a human types this — it cannot be
self-invoked.

## What this session closed

`TASK 61` — `scripts/guards/lib/path-boundary.mjs`'s `checkBashPaths` now decides write-intent
by **argument role** (a `WRITES` table + `inPlaceFlag`/`destinationArgs`/`ddTargets` helpers),
not by executable name. Bought back the 15 false-denial reads `EVAL-001` measured, and closed a
live rung-1 bypass found during checkpoint validation (`dd of=resources/x.md` was silently
allowed). `mv` still flags its source — `H-02` forbids "moves," not just writes. Full detail:
`progress/2026-08-28-08-task61-path-boundary-precision.md`. Gate: 20/20, mutation 76.15%
(floor 75.5). Whole guard suite: 830/830.

## Three new residual items, filed but not started — not in the author's stated sequence

Found during `TASK 61`'s own validation and verification, each out of that item's scope and
each independently pickable whenever convenient rather than urgently:

- **`TASK 83`** — the same "a flag placed after the target defeats `.startsWith('-')`" shape
  that `TASK 61` closed in the new `inplace`/`dest` logic still exists, untested, in the
  pre-existing `'all'`-mode loop (`rm`, `mv`, `tee`, …) at `path-boundary.mjs:175`. Cheap,
  test-only fix — same file as `TASK 61`, so worth batching with it or with `TASK 64` rather
  than a solo session.
- **`TASK 84`** — `checkBashPaths` reads `boundaries.write` only; `H-04`'s `private/` read
  boundary has **no shell vector at all** (`cat private/glossary.md` passes clean). Different
  function signature than `TASK 61`/`TASK 83` (a read-boundary check, not a write one) — do
  not fold it into either.
- **`TASK 85`** — `e2e smoke` failed once inside `gate.mjs`, with no repro captured (the
  background buffer that held the run's output was already truncated before this session
  noticed). Three of four runs since were clean. Possibly a recurrence of the Astro-preview
  daemon race `ADR-006` already documents, possibly something else — its own `Done` requires a
  repro with real detail before anyone touches code. Not urgent; watch for it recurring rather
  than chasing it now.

**If `TASK 83` or `TASK 84` is picked up before or alongside `TASK 64`**, note `G-12`: `TASK 83`
edits the same file `TASK 61` just closed (`path-boundary.mjs` + its test file) — fine solo, but
do not run it concurrently with anything else touching that file.

## Boundaries

- Never invoke a git write (`H-01`). The working tree is left uncommitted for the author —
  `TASKS.md`, the two guard files, and this session's progress logs are all still unstaged.
- If `TASK 64` is not what gets picked up next, this packet's "How to start" does not apply —
  confirm with the author rather than defaulting to it.
