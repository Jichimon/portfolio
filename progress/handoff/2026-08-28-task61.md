# Hand-off — `TASK 61`: `path-boundary` denies reads that `H-02`/`H-03` exist to permit

**Written 2026-08-28, by the session that closed `TASK 63`.** This packet is a claim, not ground truth — validate it against the real repository before acting on it (`P-04`, `P-11`).

## The goal, in one sentence

Fix `scripts/guards/lib/path-boundary.mjs` so write-intent for `sed`/`perl`/`awk`/`cp`/`mv` is decided by the actual in-place flag or destination argument, not by treating every non-flag argument of those commands as a write target — closing the false-denial pattern `EVAL-001` measured at **44% of the harness's entire denial volume**.

## How to start

```
/work-item TASK-61
```

`/work-item` carries `disable-model-invocation: true`, so a human types this — it cannot be self-invoked.

## Why this item now

Third in the author's chosen order (`65 → 63 → 61 → 64 → 66 → 67 → 75`). `TASK 63` (just closed) makes every gate step's own liveness checkable; `TASK 61` is next because it is a rung-1 boundary defect with the largest measured blast radius of anything in `EVAL-001`'s scorecard — not because it depends on anything `TASK 63` built.

## What to read, in order

1. `TASKS.md` → `TASK 61`'s own entry (~line 1931) — it already contains the full diagnosis: the exact source lines (`path-boundary.mjs:86`, `:113-118`), three real denied-read commands pulled from the trace, and the second instance found on `H-03` while verifying the first (the orchestrator's own `sed -n '9p' <trace file>` denied for "writing" to `evidence/`).
2. `scripts/guards/lib/path-boundary.mjs` — read `INPLACE`/`MUTATOR` and the argument-flagging logic named above before touching it.
3. `scripts/guards/lib/path-boundary.test.mjs` — the existing battery, so the fix extends it rather than duplicating it.

## The trap

**This is a rung-1 boundary** (`H-02`, `H-03` behind it) — do not loosen it past precision. The item's own `TASKS.md` entry states the trade-off explicitly: the current rule is deliberately over-broad, and over-denial is the *safe* direction for a rung-1 guard. The fix is **precision** (distinguish `sed -n` from `sed -i`, flag only `cp`/`mv`'s destination argument), never a broadened allowlist or a lowered rung. `TASKS.md`'s own text: *"Fix the precision, not the rung."*

## Boundaries

- Never invoke a git write (`H-01`). The working tree is left uncommitted for the author.
- This is a guard fix on a rung-1 boundary — prove it in red before trusting it (`P-14`, `T-04`): a battery showing `sed -n '1,200p' resources/site/ui.en.md` **allowed** and `sed -i 's/a/b/' resources/site/ui.en.md` **denied**, both from a neutered version of the fix.
- `TASK 61` is typed `bugfix`, touching `scripts/guards/**` — TDD is **required**, and the failing test must reproduce the false denial before the fix (`T-01`).
