# Hand-off — `TASK 88`: a render template lives in the mutation-covered surface

**Written 2026-08-30, by the session that closed `TASK 67`.** This packet is a claim, not ground truth — validate it against the real repository before acting on it (`P-04`, `P-11`).

## The goal, in one sentence

Decide, and make true, whether `renderLedger` (`scripts/guards/lib/status-history.mjs`) — a render template that happens to live inside the mutation-covered `lib/**` surface — is excluded from mutation with a written reason or kept in with its remaining `CallExpression` survivors actually killed, so `scripts/guards/lib/status-history.mjs` carries no surviving mutant a reader has to re-triage.

## How to start

```
/work-item TASK-88
```

`/work-item` carries `disable-model-invocation: true`, so a human types this. `TASK 88` is typed `maintenance`, so — same as `TASK 67` — **there is no spec**: the reconciled diff (either the exclusion decision or the killed mutants) is the artifact the author approves at the checkpoint.

## Why this one, and why now

It is `TASK 66`'s own residual, placed immediately after `TASK 67` in the run order (position 9a), and it is the only remaining open item that touches a file `TASK 67` did not — no ownership overlap with anything just closed.

## What to read, in order — the extract, not the sources

1. **`TASKS.md`, the `TASK 88` entry** (search `## TASK 88`). Short — carries the `Done`, the two named mutator classes (`StringLiteral`, already suppressed with a reason; `CallExpression`, ~40 survivors, not suppressed), and the three constraints at the bottom.
2. **`.claude/rules/30-testing.md`, the `D3` scoping line**: *"Mutating render templates produces equivalent mutants and noise; parsing, joining and validating is real logic a mutant can break."* That is the test this item's decision has to satisfy either way.
3. **`scripts/guards/lib/status-history.mjs`** — the file itself, specifically `renderLedger`. Read it before deciding; the entry's own analysis (data rows worth keeping covered vs. prose lines that are pure render noise) needs checking against the current function, not trusted as still accurate.
4. **`stryker.config.mjs`** — how an existing suppression is written (`// Stryker disable next-line <mutator>: <why>`), since `mutation-suppressions.test.mjs` fails a reasonless one (`T-03`).

## The traps

- **Do not solve this by lowering the mutation floor.** It sits at 76.0 as of `TASK 66`, against a measured 76.55–76.56% across this session's two full gate runs. `TASKS.md`'s constraints section for this item says so explicitly, and `T-03` makes a suppression carry its reason at the mutant, never a moved number.
- **A `!` glob entry naming `status-history.mjs` is a roster, not a decision** (`P-13`). If the answer is exclusion, it has to be a declared *property* — "this function renders" — not a filename dropped into a config array.
- **The `CallExpression` survivors are not uniform.** Some delete a `push` of pure prose (noise); others delete a `push` of an actual data row (`` L.push(`- ${t.date} · ${t.id} · ${t.from} → ${t.to}`) ``), which the existing tests do kill and which is the actual point of the artifact. A blanket per-mutator suppression would quietly also stop covering the data rows — read the entry's own paragraph on this before choosing a shape.

## What already exists, so it is not rebuilt

- The `StringLiteral` half is **already handled** — suppressed at the mutant with a written reason. Only the `CallExpression` half (~40 survivors) is open.
- `mutation-suppressions.test.mjs` already enforces that every suppression carries a reason — this item does not need to build that check, only use it correctly if it goes the suppression route.

## The state you are inheriting

`TASK 67` is `DONE`. Gate **21/21, exit 0** on the final run; mutation **76.56%** against the 76.0 floor. The working tree is **uncommitted** — the human owns the commit (`H-01`, `D2`), and it now also carries `TASK 67`'s edit plus everything still uncommitted from `TASK 66` per that item's own hand-off.

**One flake fired twice now, still not this milestone's blocker.** `TASK 89` (`component tests`, zero tests collected, `TypeError: Cannot read properties of undefined (reading 'config')`) recurred during `TASK 67`'s own gate run — second data point, recorded in `TASK 89`'s `TASKS.md` entry. It is **not placed in the run order** and should not be chased; if it fires a third time, that is more evidence for whoever eventually picks `TASK 89` up, not a reason to retry past it (`T-06`).

**A run-order staleness was found and fixed in passing:** row 7b (`TASK 83`) read as not-`DONE` in the table while its own entry had read `DONE` since 2026-08-29. Corrected this session. Worth a glance at the rest of the run-order table before trusting any other row's status column at face value.

## Boundaries

- Never invoke a git write (`H-01`). The human owns commits.
- `TASK 88` owns **one file's mutation posture** — `scripts/guards/lib/status-history.mjs` (and its test file, if the fix is added kills rather than a suppression). Do not widen into `TASK 84`'s or `TASK 75`'s scope, both still open and unrelated (`G-12`).
- Do not lower `stryker.config.mjs`'s floor to make this item easier. If the honest number after the fix is lower than 76.0, that is a finding, not a knob to turn.
