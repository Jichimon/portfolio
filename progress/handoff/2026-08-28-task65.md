# Hand-off — `TASK 65`: two gate checkers pass on an artifact they cannot classify

**Written 2026-08-28, by the session that closed `TASK 77`.** This packet is a claim, not ground truth — validate it against the real repository before acting on it (`P-04`, `P-11`).

## The goal, in one sentence

Two gate checkers — `check-evals` and `check-procedures` — currently pass on an artifact whose real state they cannot classify; both get fixed to derive what the artifact *is* from a property it declares, rather than from a shape it happens to match.

## How to start

```
/work-item TASK-65
```

`/work-item` carries `disable-model-invocation: true`, so a human types this — it cannot be self-invoked.

**Run this on Sonnet.** It is `bugfix` work in the mutation-covered surface (`scripts/guards/lib/**`): `T-01` requires a failing test that **reproduces the bug** before the fix, for both clauses. That is TDD with a red battery — the case `ADR-009` §6 allocates to Sonnet.

## Why this item now

Next in the author's chosen order (`65 → 63 → 61 → 64 → 66 → 67 → 75`). It is also, right now, **the only red step in the gate**: `node scripts/gate.mjs` reads 19 of 20, with `procedures` failing on exactly one file — `progress/2026-08-27-13-eval001-workitem-extract.md`, a generated artifact (reproducible byte-for-byte from a read-only script) that `check-procedures` currently treats as a work log missing its `done:` block, because it cannot tell the two apart. That is clause 2 below.

## What to read, in order

1. `TASKS.md` → `TASK 65`'s entry — both clauses, the done clause for each, and the note on why it is one item and not two.
2. `TASKS.md` → the retired `TASK 68` entry, immediately below `TASK 65`. **Read this even though it is retired** — it records why the three obvious fixes for clause 2 are all wrong, and re-deriving that would waste the item's whole budget on a dead end. Excerpt, so you don't have to go looking: an exclusion roster is `INC-07`'s shape and the guard's own rationale refuses it; moving the file breaks `check-docs` instead (both the scorecard and `TASK 60`'s log cite its path); emitting a `done:` block from the generator makes the guard green by making the artifact lie.
3. `evaluation-cases/EC-014-*.yaml` — the live instance for clause 1: `proof: none`, `outcome: Gap`, `proof_reason` claiming a fix "not implemented yet," when the fix (`TASK 18`) has been `DONE` for some time.
4. `scripts/guards/lib/evals.mjs` and `scripts/guards/gate/check-evals.mjs` — clause 1's surface.
5. `scripts/guards/lib/procedures.mjs` and `scripts/guards/gate/check-procedures.mjs` — clause 2's surface. Note while reading: this module also owns `iterationBuckets`/`validateIterationSplit`, added by `TASK 72` — unrelated to this item, do not touch it incidentally.

## The trap

Both clauses are the same defect on two different modules: **a checker deciding what an artifact *is* by a shape it happens to match, rather than by a property it declares.** Resist any fix that widens a roster or adds a filename exception — that is `INC-07` reproduced inside the exact checker family built to catch it (`P-13`).

## What already exists, so it is not rebuilt

- `check-evals` already has its required-field derivation (`guards.config.json`'s `evals.filledLater`, subtracted from the template rather than listed) — clause 1 extends that pattern, it does not replace it.
- `check-procedures` already derives `iterationBuckets` from the skill file's own headings rather than a hardcoded list (`TASK 72`) — clause 2's "a marker is a declared property of the file, not a list of filenames" is the same idiom applied one level up.

## Boundaries

- Never invoke a git write (`H-01`). The working tree is left uncommitted for the author.
- Never write to `resources/`, `evidence/`, or read `private/` (`H-02`, `H-03`, `H-04`).
- `TASK 65` is typed `bugfix`, so no spec is required (`H-05`'s `specRequiredFor` only fires for `feature`/`migration`) — but `T-01` still requires a bug-reproducing failing test before the fix, for both clauses.
- A write-capable delegation still needs at least one work-item id in its brief — cite `TASK 65`.
- Do not let a bare `TASK 22`–`TASK 26` appear in any delegation brief — all five are typed `feature`, and `extractWorkItems` scans the whole brief text; naming one triggers the approved-spec check for no reason. Refer to them descriptively if you ever need to.

## Before you close

Same convention `TASK 79`/`80` established: write the next hand-off packet if the following item will start in a fresh session, and say the hand-over out loud in the terminal — the cut, the paste-ready prompt, and the tier with its reason.
