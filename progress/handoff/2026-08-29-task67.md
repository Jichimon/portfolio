# Hand-off — `TASK 67`: `harness-evaluator`'s budget is conditional, and the role file does not say so

**Written 2026-08-29, by the session that closed `TASK 66`.** This packet is a claim, not ground truth — validate it against the real repository before acting on it (`P-04`, `P-11`).

## The goal, in one sentence

State, in `.claude/agents/harness-evaluator.md`, that the measured ~37-of-60 turns held **only because the corpora were precomputed**, so the next brief that hands over raw corpora reads 60 as sufficient when the measurement says nothing of the kind.

## How to start

```
/work-item TASK-67
```

`/work-item` carries `disable-model-invocation: true`, so a human types this — it cannot be self-invoked. `TASK 67` is typed `documentation`, so **there is no spec**: the reconciled document is the artifact the author approves at the checkpoint.

## Why this one, and why now

It is next in the author's run order (position 9, after `TASK 66`), it is still `TODO`, and — the reason it should not slip — **this session edited the very file `TASK 67` owns.** Every session that touches `harness-evaluator.md` without closing `67` widens the gap between what that file says about its budget and what is true.

## What this session changed in that file, and why it matters to yours

One line, at the end of the Bootstrap list:

> **6. The status-history ledger**, if the brief names one. It is `K2`'s whole substrate… **You hold no `Bash`, so you cannot derive it** — it is handed to you as a file, generated by the orchestrator before you are dispatched.

**That is a third corpus, and `TASK 67`'s entry says "both".** The entry was written on 2026-08-27 when there were two — the trace index and the work-item extract. There are now three, and the one added is cheap to generate and cheap to read, but it is not zero. Say **three**, or say "every corpus the brief names", rather than copying "both" forward into a claim that was already true of a different number.

## What to read, in order — the extract, not the sources

`P-09`'s reading half: a packet that says *go read `TASKS.md` and work it out* has handed over an unbounded read. These four are the whole corpus for this item.

1. **`TASKS.md`, the `TASK 67` entry.** Short. It carries the `Done` verbatim and the three items it explicitly does *not* duplicate (`TASK 14`, `GAP-12`, `TASK 11`).
2. **`.claude/agents/harness-evaluator.md`** — the file you are changing. The budget paragraph is the third one, beginning *"Your budget is 60 turns, and that number is measured rather than guessed."*
3. **`.claude/rules/10-process.md`, `P-09`** — it already carries the measurement `TASK 67` has to cite: **0 of 3 slices cut when briefed with a pre-written extract, against 1 of 1 cut at ~100k tokens when told to go read three spec files.** Quote the numbers from there rather than re-deriving them.
4. **`progress/evaluation-results/EVAL-001-first-non-harness-work.md`** — where the ~37-of-60 observation came from. Confirm the figure against it before restating it (`C-01`: an unmeasured number must not be presented as measured, and the harness's own numbers are held to that too).

## The traps — what would otherwise be rediscovered the expensive way

- **`check-agents` reads a backticked token in the Bootstrap section as a path claim.** This session lost a gate run to it: the phrase `` `git log -- TASKS.md` `` in bootstrap item 6 failed with *"Bootstrap names `git log -- TASKS.md`, which does not exist — the role would bootstrap into a void"* (`INC-04`). Correct behaviour. If your new wording puts a command or a shaped path in that section, phrase it in prose instead. Elsewhere in the file it is fine.
- **The budget number itself is `maxTurns`, which is the one budget the runtime actually enforces** (`G-06`). Do not restate it as if the other three were enforced too — `maxToolCalls`, `maxRuntime` and `maxRetries` are *observed from the trace at wrap-up*, and `maxCost` is not available as a control at all. `G-06`'s row is long and precise; match it rather than paraphrasing it.
- **`check-agents` also asserts the six posture dimensions and that the bootstrap paths resolve** (`G-05`). Run `node scripts/guards/gate/check-agents.mjs` after every edit to that file — it is two seconds and it is the check that catches the class of mistake above.
- **`documentation` was absent from the register's type table until `TASK 74` reconciled it.** It is there now, and `parseWorkItemTypes` derives from that table — but it means this item's type has been read correctly for exactly two days. Confirm `parseWorkItemTypes` still returns `documentation` for `TASK-67` before assuming the delegation gate classifies it.

## What already exists, so it is not rebuilt

- The measurement is **already recorded** in `P-09` and in `EVAL-001`. This item writes it into the role file; it does not take a new measurement.
- `ADR-009` already decided that a deterministic script assembles the corpus and that neither the orchestrator nor the agent holds it. The role file should point at that decision rather than re-argue it (`G-10`).

## The state you are inheriting

`TASK 66` is `DONE`. Gate **21/21, exit 0**; guard suite **907/907**; mutation **76.55%** against a floor raised this session to **76.0** (`T-03`'s ratchet, in `stryker.config.mjs`, with `30-testing.md`'s row reconciled). The working tree is **uncommitted** — 12 modified, 5 new — and the human owns the commit (`H-01`, `D2`). Full detail: `progress/2026-08-29-05-task66-k2-substrate.md`.

**Two residuals were filed, neither started.** `TASK 89` — the `component tests` step failed **once**, on the fourth consecutive gate run of the session, at module evaluation with **zero tests collected** (`TypeError: Cannot read properties of undefined (reading 'config')`, followed by Vite's own *"Re-optimizing dependencies"*). It passes 15/15 standalone and passed the three runs before it. It is a runner flake in a tier `TASK 66` touches no file of, it has its output captured — which `TASK 85` does not — and it is **not** placed in the run order. **If it fires during your session, do not retry past it**: `T-06` makes a flake a finding, and `TASK 89` is where the second data point belongs.

And `TASK 88` — `renderLedger` is a render template living inside the mutation-covered surface, which is the shape `D3` scoped mutation away from. Placed at 9a, after this item. It is not a blocker for `TASK 67` and the two share no file.

## Boundaries

- Never invoke a git write (`H-01`). The human owns commits.
- `resources/**` is read-only (`H-02`); `evidence/**` is hooks-only (`H-03`); `private/**` is never read by a delegated role (`H-04`).
- `TASK 67` owns **one file**. `TASK 14`, `GAP-12` and `TASK 11` are referenced by its entry and are **not** in its scope — the entry says so explicitly, and widening it is `INC-01`'s shape.
- Do not change the 60-turn number. The item's `Done` is to state the **condition** under which it was measured, not to re-tune the budget.
