# Hand-off — `TASK 76`: the English half of the Spanish rewrite, so publication can run

**Written 2026-08-31, by the session that worked `TASK 89` and shipped `TASK 103`.** This packet is a claim, not ground truth — validate it against the real repository before acting (`P-04`, `P-11`). Where it says *verified*, this session ran the command and the output is quoted.

## The goal, in one sentence

Produce the **author packet** that brings the English half of `about`, `experience`, `ui` and the two case studies back into agreement with the rewritten Spanish, with the four scale figures settled first — so `TASK 30` stops being blocked.

## How to start

```
/work-item TASK-76
```

`/work-item` carries `disable-model-invocation: true`, so **a human types this.** `TASK 76` is typed `content`, so **there is no spec** — the artifact the author approves is the drafted copy itself.

## Why this one and not `TASK 30`

**Because the register's recommended order and `TASK 30`'s own entry disagree, and the entry wins.** The order line reads *"→ `TASK 30`/`TASK 32` → …"*, but `TASK 30`'s heading says **`blocks: TASK 76`** in its own body: *"Publishing now makes a bilingual portfolio that contradicts itself public, in the locale most of the target audience reads. That is not a thing to fix after the first push."*

So the real path is **`TASK 76` → `TASK 30` → `TASK 32`**, and a session that opens `TASK 30` first walks into that wall after doing the git-history re-verification for nothing.

## The extract — read this, not the sources first

**Verified this session.** Do not re-derive; do confirm anything you intend to publish.

### The one thing that makes this item different from every other content item

**No agent can write `resources/**`.** `H-02` denies it at rung 1, by a `deny` rule *and* the `resources-readonly` guard. So the deliverable is **not an edit** — it is an **author packet**: `progress/handoff/<date>-task76-content.md`, carrying the drafted English for the author to apply. `progress/README.md` names this form explicitly and `progress/handoff/2026-08-26-task26-content.md` is the worked example to copy.

A session that plans to "fix the English files" will discover this at its first write, having spent its context drafting into a tool call that gets denied.

### Settle the numbers before writing a word of English

`TASK 76`'s own entry is emphatic and it is right: four **distinct** subjects are currently collapsed into one figure across three files.

| Subject | Figure | State |
|---|---|---|
| Bank mobile app — **total** users | **≥2 million** | correct only in `experience.es.md:22` |
| Bank mobile app — **active** users | hundreds of thousands | correct and properly qualified in `mobile-banking-platform.{en,es}.md` — **this file is the model to copy** |
| QR Business module | 100,000 in the first three months | absent from `experience.en.md` |
| NICE CX platform | millions | both locales already agree — a **different** system, must not be conflated with the bank's |

`about.{en,es}.md`, `experience.en.md` and `profile-README.md` state the **active**-user figure as if it were the **total**. Settle this first, or the understated number gets baked into a second locale. Then `C-03` applies: a corrected fact propagates to every derived page **in the same change**, both locales.

### The object list in the register is incomplete, and this session widened it

`TASK 76`'s table enumerates `about.en.md`, `experience.en.md` and `ui.en.md`. The working tree also carries **two uncommitted Spanish-only edits the item never listed**:

- `resources/case-studies/mobile-banking-platform.es.md` — `subtitle` rewritten; `stack` gains Flutter, Android, iOS, Postgres, RabbitMq and **drops MassTransit and Polly**; `skills` gains `micro-servicios`, `clean-architecture`, `DDD`; prose reflowed. The `.en.md` still carries the old stack.
- `resources/case-studies/legacy-payment-data-migration.es.md` — ~81 lines changed, largely prose.

The register was updated this session to say so. **Re-check `git status` before slicing** — the author was still editing while this packet was being written, and by 12:2x had also touched `mobile-banking-platform.en.md`.

### A live `C-01` finding in the working tree — read this before you settle the numbers

Both locales of `mobile-banking-platform` now say *"more than a million **active** users"* / *"más de un millón de usuarios **activos**"* in the body, while **both frontmatters still carry `scale: "+100.000s"` / `active users`**. So the page contradicts itself in both locales, and the figure matches neither recorded subject — the register has **≥2 million = total** and **hundreds of thousands = active**. This is the very file the register names as *the model to copy* for a correctly qualified figure.

Do not "fix" it by editing `resources/**` — `H-02` denies that at rung 1, and it is the author's in-flight work. It goes in your packet as the first question.

### `check-content` passes on the whole divergence

Verified 2026-08-31: `20 content file(s) · 9 locale pair(s) across en/es · 2 reasoned exemption(s)`, **exit 0**. The parity assertion covers the pair existing and its universal keys, not `stack`, `skills`, `subtitle` or prose. So **the gate is not a safety net for this item** — it will stay green while the two locales say different things. That gap is `TASK 75`, which is a separate `TODO` and is *not* this item's job to close.

## The traps

- **`H-02` is rung 1.** The deliverable is a packet, not an edit. See above — this is the trap that costs a whole session.
- **Slice by objects, not by surface** (`P-09`). *"The English content"* is a surface. *"`about.en.md` — the `h1`, the two captions, the opening section, the judgment paragraph, the university paragraph"* is an object list, and the register already writes most of it out.
- **Two items inside the divergence are not translation.** `experience.es.md` **dropped the thesis anchor** (*"That gap is the one I have been working in ever since"*) — restoring it **in Spanish** is part of this item. And `ui.es.md` lost *"· full overlap with US business hours"* from `rail.timezone`, which is a selling point for exactly the target roles, dropped in the locale that should keep it. **Both fixes run the other way, ES ← EN**, and a session that reads this item as "write the English" will miss them.
- **`C-01` over a plausible number.** If a figure is needed and does not exist, `[NEEDS INPUT] <question>. Why it matters: <one line>.` — never an estimate. `INC-09` is what that rule is made of.
- **Two traceability records became false** and must be reconciled in the same change (`P-07`) — the register names them.
- **`C-13`**: simple past for completed work, B2–C1 register, flat and direct. Flat technical English reads more senior than ornate English.

## What already exists, so it is not rebuilt

- `TASKS.md` § `TASK 76` — the file-by-file divergence table, the scale table, the two non-translation findings, and now the two case studies this session added.
- `TASKS.md` § `TASK 30` and § `TASK 32` — the publication items this unblocks, with their own constraints.
- `progress/handoff/2026-08-26-task26-content.md` — the author-packet form to copy.
- `resources/case-studies/mobile-banking-platform.{en,es}.md` — the **model** for a properly qualified scale figure.

## What comes after, so the path is visible

- **`TASK 30`** — `maintenance`. The author performs every git write (`H-01`); an agent prepares and reads history but does not create the remote, commit or push. **Re-verify the history immediately before pushing** — `ADR-005`'s check was true in August, and a push publishes whatever is there *now*.
- **`TASK 32`** — typed **`feature`**, so it **does** need `docs/specs/SPEC-TASK-32-*.spec.md` and the checkpoint (`P-02`), and `H-05` stops write-capable delegation until `approved_version == version`. CI is the only deploy path from the first deploy; the author creates `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` as repository secrets, because no project credential enters the session environment (`G-08`).

## The state you are inheriting

`TASK 103` is `DONE` — a config file no longer garbage-collects a build cache in every process that loads it. `TASK 89` was worked and **left `TODO`**, reported `partial`: both recorded candidates refuted, and its remaining question narrowed to one — *what produces two `@vitest/runner` instances*.

**`TASK 89`'s flake reproduced during that session's wrap-up**, on the fifth full gate run of the day, and the timestamps proved the `Re-optimizing` line belongs to `astro check` five seconds later rather than to Vitest. **Expect roughly one gate run in seven to fail `component tests`.** When it does: it is the known flake, not your change — re-run the step alone to confirm (it passes 15/15), say so in your `done` block rather than letting a red step become background noise, and do not "fix" it in passing.

**Gate 21/21, real exit code 0** on the tree as delivered, mutation **78.57%** against the 77.0 floor. The working tree is **uncommitted** and carries this session's eight files plus the author's own two under `resources/` — the human owns the commit (`H-01`, `D2`).

**Model tier: Opus.** `ADR-009` §6 puts judgment work on Opus and production work on Sonnet. This is content authorship against a professional thesis, with a live `C-01` judgment on four figures and two ES-side restorations that require reading intent rather than diffing text. That is the expensive half, and it is not spent yet.

## Boundaries

- **Never write to `resources/**`** (`H-02`). The deliverable is an author packet.
- **Never invoke a git write** (`H-01`). The human owns commits.
- **Never write to `evidence/`** (`H-03`).
- **Do not close `TASK 75` here.** The parity guard's gap is real and observed twice, but this item owns the **copy** and `TASK 75` owns the **check**. Fixing one does not fix the other, and folding them makes "done" mean two things (`INC-01`).
- **Do not fill a missing figure with a plausible one** (`C-01`, `C-07`). If sanitization would make a claim useless, stop and say so.
