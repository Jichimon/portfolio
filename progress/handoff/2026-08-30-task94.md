# Hand-off — `TASK 94`: shell-level expansion becomes a stated residual

**Written 2026-08-30, by the session that closed `TASK 95`, `TASK 96` and `TASK 99`.** This packet is a claim, not ground truth — validate it against the real repository before acting (`P-04`, `P-11`). Where it says *verified*, this session ran the code or the shell and the output is quoted.

## The goal, in one sentence

Name shell-level expansion and runtime data flow — glob, variable, `cd`, pipe-to-dynamic-command, and the obfuscated-wrapper forms this session left open — as a **stated, bounded residual** in `docs/harness/architecture.md` §L, so the harness claims what it enforces and nothing more (`G-07`).

## How to start

```
/work-item TASK-94
```

`/work-item` carries `disable-model-invocation: true`, so **a human types this**. `TASK 94` is typed `documentation`, so **there is no spec and TDD does not apply** (`T-01`) — the reconciled prose is the artifact the author approves at the checkpoint. Declare the non-applicable dimensions out loud in the done block; silence reads as coverage (`P-03`).

## Why this one, and why now

**It retires a divergent series rather than adding to it.** `INC-17`, transcribed this session: eight of the twelve items closed between 2026-08-29 and 2026-08-30 were command-decomposition bypasses in `shell.mjs`. Every one was real. None advanced either project goal. The series is divergent by construction — `TASK 84`'s audit opened `92`, `93`, `94`; `95`'s opened `96`; `96`'s opened `97` and `98` — because that surface emulates *other programs'* grammars and therefore has no terminal state.

`TASK 94` is the exit. Once the claim is honestly scoped, `TASK 91`, `TASK 97` and `TASK 98` stop being silent holes and become documented limits, which is what they already are in practice: an adversary writes `env --s`, a mistaken agent writes `git commit -m x`, and this project has one operator and no adversary.

**Read `TASKS.md` § "Goal alignment — the triage of 2026-08-30" before deciding to do anything else.** It sorts all 26 open items against the project's two goals. The recommended order after this item is `TASK 89` → `TASK 30`/`32` → `TASK 100` → `TASK 9`.

## The extract — read this, not the sources first

**Verified live against this machine's real bash this session.** Do not re-derive it; do confirm any line you intend to publish.

Forms that execute and that the guards do **not** catch, and will not after this item — that is the point:

```text
sudo -s "git commit -m x"          H-01   POSIX sudo passes -s/-i args to $SHELL -c   <- TASK 97, UNPROVEN here
doas -s "git commit -m x"          H-01   same shape                                  <- TASK 97, UNPROVEN here
powershell -EncodedCommand <b64>   H-01   base64 UTF-16LE, outside EVAL_FLAGS         <- TASK 98, CONFIRMED executing
echo "cat private/x" | xargs -0 sh -c     H-04   payload arrives on STDIN             <- structural, unfixable by argv analysis
```

**`TASK 97` is recorded as unproven in execution and must stay that way unless someone verifies it.** The only `sudo` on this machine is Windows `C:/WINDOWS/system32/sudo`, which has a different option set and triggers elevation; `doas` is absent. The finding rests on reading the guard plus documented POSIX semantics. Publishing it as demonstrated would be `C-01`'s exact failure — an unmeasured claim presented as measured.

**The `xargs`-from-stdin case is the one that belongs in §L most**, because it is not a decomposition gap at all: the dangerous token never appears in argv, so no argv-analysing guard can see it. Closing it would need a sandbox that executes the pipeline, or accepting it as a permanent limit of a non-executing, `policy-controlled` guard.

**Already closed this session, so §L must not claim these as open:** `env sh -c "…"` and every `DIRECT_WRAPPERS` chain (`TASK 95`), and `env -S` / `--split-string` in all six spellings including abbreviations down to `--s` (`TASK 96`).

## The traps

- **The pull to fix what this item documents is `INC-17` itself.** `TASK 94` names residuals; it does not close them. If the session finds itself editing `shell.mjs`, it has become the ninth bypass item. `P-19` is the rule; re-read it before opening any adjacent work.
- **`architecture.md` §L was checked this session and is silent on these forms** — the gap is real, not assumed. But `checkBashPaths`'s own docstring already states a bounded scope ("does not and cannot catch a script the agent wrote and then executed"). Reconcile the two so they do not drift into two competing statements of the same limit (`G-10`).
- **Do not claim a rung the residual has not earned.** `G-07` exists precisely because a false 🔒 retires a human eye that is still needed. The honest form names what is *not* covered as loudly as what is.
- **`TASK 97` and `TASK 98` must be split out as their own entries, not folded into the prose** (`P-06`). They already exist in `TASKS.md`; check they still read correctly against whatever §L ends up saying, rather than duplicating them into it.

## What already exists, so it is not rebuilt

- `TASKS.md` § "Goal alignment" — the sorted board, written this session.
- `docs/harness/architecture.md` §C `INC-17` — the incident behind `P-19`.
- `progress/2026-08-30-06-task95-*.md` and `-07-task96-*.md` — the real-bash evidence for every form named above, with the spellings that were tested and the ones that were not.
- `scripts/guards/guards.config.json` → `evals.excluded` — `INC-17`'s exclusion, with its return trigger. The pattern to copy if §L needs one.

## The state you are inheriting

`TASK 95`, `TASK 96` and `TASK 99` are `DONE`. **Gate 21/21, real exit code 0**; mutation **78.57%** against the 77.0 floor, zero survivors in the new code. The working tree is **uncommitted** and also carries `TASK 92`/`93` — the human owns the commit (`H-01`, `D2`).

**Model tier: Opus.** `ADR-009` §6 puts judgment work on Opus and production work on Sonnet. This item's whole deliverable is a scoping judgment about what the harness may claim, in a repository about to become public — getting it wrong produces an overclaim, which is the failure `G-07` exists to prevent and the one a cheaper tier is least likely to catch itself making.

## Boundaries

- **Never invoke a git write** (`H-01`). The human owns commits.
- **Never read `private/**`** (`H-04`). Use `private/__probe_does_not_exist__` in any example; the guards are pure functions over command text and need no real file.
- **Do not re-open the bypass series** without a reason that names a goal (`P-19`). A finding is evidence that something is true, never evidence that fixing it serves the goal.
- `TASK 94` owns `docs/harness/architecture.md` §L and the `TASKS.md` entries for `91`/`97`/`98`. It does not own `shell.mjs` (`G-12`).
