# Author packet — TASK 104, the remaining English + one stale note

Written 2026-09-01. This is a **claim, not an edit** — `H-02` denies
`resources/**` to every agent, this session included, so nothing below has
touched the files yet. Four blocks, each labeled with its target file and
exact insertion point. Paste as-is or rewrite — these are drafts, not orders.

Everything else `TASK 104` opened is already resolved in the working tree
(points 1, 2, 4, the "US" half of point 6, and both minors) — nothing to do
there.

---

## 1 — Point 3: `multi-tenant-biometric-attendance.en.md`

**Where:** end of the modules paragraph in `## Approach` → "Modular monolith,
with microservice-shaped boundaries", the sentence starting "The modules:
organisational data...".

**Current text ends:**

> The modules: organisational data, identity and access, attendance, and
> internal communications. Each with its own domain, its own persistence
> access, and an explicit contract to the others.

**Append:**

> A vertical-slice approach, ambitious for a technology that tempts you toward
> horizontal slices and solution-wide clean architecture. Here I pushed clean
> architecture down into each module. If you ask me, it was too ambitious, and
> that is why it took us so long. Over time I learned to be more pragmatic.

Translates the passage the Spanish twin now carries in full: *"Un enfoque con
slices verticales, ambicioso para una tecnología que te seduce con slices
horizontales y clean architecture a nivel solución. Acá se trasladó clean a
cada módulo. Si me preguntás, es muy ambicioso y por eso nos demoramos mucho.
Con el tiempo aprendí a ser más pragmático."*

---

## 2 — Point 5: `otp-provider-decoupling.en.md`

**Where:** end of the `## Constraints` bullet list, after "A security hole
here meant a user could defraud the bank directly."

**Add bullet:**

> - The team assigned to me was two people at 40% each — they kept working
>   with their regular teams the rest of the time — so operating capacity was
>   limited.

Translates the bullet the Spanish twin now carries: *"El equipo que me
asignaron eran 2 personas al 40% (seguían trabajando con sus equipos igual),
así que teníamos capacidad de operación limitada."* (Two small typos in the
Spanish as written — *asignarón* → *asignaron*, *asi* → *así* — noted in case
you want to fix them while you're in the file; not blocking.)

---

## 3 — New section: `mobile-banking-platform.en.md`

**Where:** between `## Architecture` (ends "...The bank's whole legacy estate
sits on-premise as well.") and `## Services I designed and owned`, mirroring
where `## Despliegue en Nube` now sits in the Spanish twin.

**Insert:**

> ## Cloud Deployment
>
> All the infrastructure ran on AWS. Each team managed its own EKS cluster,
> where pods could scale out horizontally up to 5 replicas depending on the
> load they received, and scaled back down to 1 or 2 when load dropped,
> depending on the service. Each service had its own resource specs, sized to
> the load it had to handle vertically.
>
> Everything was managed through Terraform. Deployments ran through Azure
> DevOps, with Azure Application Insights for observability.

Translates the section added to the Spanish twin:

> "## Despliegue en Nube
>
> Toda la infrastructura se encontraba hosteada en AWS. Cada equipo manejaba
> sus propias EKS, donde según la carga que recibía cada "pod" se podían
> escalar horizontalmente hasta 5 de ellos. y si la carga bajaba, se mantenía
> funcionando 1 o 2 dependiendo del servicio. Cada servicio tenía sus propias
> especificaciones acorde a la carga que debía soportar verticalmente.
>
> Todo se controlaba a través de terraform. Los despliegues eran realizados a
> través de AzureDevOps. Usando Azure Application Insights para
> observabilidad."

No new fact outside the stack list — AWS, Azure DevOps and Application
Insights are all already declared in this file's frontmatter `stack:` (`C-04`).

---

## 4 — Point 6's residual: `ui.es.md`, `## Un detalle tipográfico`

**Not a translation — a proposed fix**, flagged separately because you didn't
ask for this one directly. It's what point 6 in `TASKS.md` actually asks for:
the note under this heading still describes a decision that no longer applies.

**Why it's stale:** the note says the file uses `EE. UU.` with a normal space
where the artboard used a hard space. But `rail.timezone` doesn't contain any
form of `EE.UU.` any more — it now reads `"GMT-4 · solapamiento completo con
horario laboral en US"`. The note describes a string that exists nowhere in
the file (same failure `TASK 104` opened this question over the first time,
`P-07`).

**Current section:**

```
## Un detalle tipográfico

El artboard escribe `EE.&nbsp;UU.` con espacio duro. Acá va con espacio
normal, igual que en `home.es.md`, para que las dos fuentes de esa misma
línea digan lo mismo.
```

**Proposed replacement:**

> ## Un detalle tipográfico
>
> `rail.timezone` deja **US** sin traducir, no `EE. UU.` — más corto, y evita
> la duda entre espacio duro y espacio normal que tenía el artboard. La misma
> razón por la que `rail.role` se queda en inglés.

If you'd rather drop the heading entirely instead of replacing it, that's a
legitimate call too — nothing else in the repository points at this heading
(`grep` confirms only this file and the superseded `task76-es` staging copy
mention it), so removing it breaks no citation.

---

## Once applied

`check-content` and `check-terms` should both stay green — none of this adds
a banned term or a new frontmatter shape. After pasting all four in, tell me
or run `git diff` yourself so `TASK 104` can close against the applied tree,
the same way `TASK 76` did.
