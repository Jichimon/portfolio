# 2026-08-27 · Session 13 — `TASK 60`: run `EVAL-001`, the milestone's harness-scoring half

**Task:** `TASK 60` · `harness` · the second half of the localhost milestone. Produce `progress/evaluation-results/EVAL-001-<slug>.md` in `EVAL-TEMPLATE.md`'s fixed shape, scoring the harness against the first non-harness work it has ever run, and file every `Gap` as a tracked work item.
**Status after this session:** `DONE`. The scorecard exists, all 14 cases are scored, every gap is filed as `TASK 61`–`TASK 69`, and the gate ran with two red steps that are diagnosed and owned rather than retried. This log was written as a skeleton **before** the delegation, not reconstructed after it (`P-05`, and `P-09`'s one measured mitigation — eight cut runs, eight surviving logs).

## The frame, fixed before anything was scored

`/evaluate-harness` step 1 says to decide the frame first and write it down, so that a scorecard cannot be quietly reshaped by what it finds. This is that record.

### Which runs are in scope

**Post-baseline runs only** — the 33 run directories whose first event postdates the last event of the two directories `EVAL-000` scored, plus one edge case named below. Decided with the author before the delegation.

- `EVAL-000` scored `5751ce4c-…` and `2ac4fd9f-…`, whose last event is `2026-08-19T16:08:28.265Z`. Those two are the **comparison baseline**, not part of this count — including them would double-count their denials in `V` and their defects in `K3`, manufacturing a trend out of re-reading the same events.
- The partition is drawn from event `ts` fields by `progress/2026-08-27-13-build-workitem-extract.mjs`, §1, not from directory mtimes, which a checkout rewrites.
- **One directory is in neither set:** `90f82190-6db7-4a3c-8b7a-fe66b50912a3`, a 7-event orchestrator run from `2026-08-19` that `EVAL-000` explicitly excluded as *postdating its analysis window*. It has therefore never been scored by anyone. It is handed to the evaluator as an open question rather than silently absorbed.
- **This session's own directory, `21861e1c-40fc-4c83-9d34-fce7fc364625`, is inside the scored window and is being appended to while it is read.** `EVAL-000` hit the same reflexivity and handled it the same way: the trace can observe itself, but it cannot observe its own ending.

### The posture each run ran under

| | |
|---|---|
| `enforcement_environment` | `policy-controlled`, in every header that carries the field. No OS sandbox — `G-07` caps the claim there. |
| `permission_mode`, corpus | **9 of 176 headers carry a real value** (5 `auto`, 4 `plan`); the rest read `unknown`. **No run in the corpus shows `bypassPermissions`.** |
| `permission_mode`, this session | `21861e1c-…/orchestrator.jsonl` — `unknown` at `seq:1` (`reason: startup`), then observed `plan` at `seq:7` and `acceptEdits` at `seq:49`. Recorded because `G-04` says a higher settings scope can override the project pin out of band and the evaluator should see which mode actually ran. |
| What that permits | The same weaker claim `EVAL-000` made: no run is *known* to be `bypassPermissions`, and the assurance for the `unknown` majority comes from `G-04`'s static `disableBypassPermissionsMode: "disable"` pin plus the `runtime boundary` gate step — not from an observed header. The 9 observed values are new since the posture-header slice landed on 2026-08-27; before it, 118 of 118 read `unknown`. |

No run is excluded on posture grounds. If one had been, it would be named here.

### Baseline or evaluation

**An evaluation, not a baseline.** `EVAL-000` was a retrospective replay of transcribed incidents against a harness that had never run a non-harness work item; it declared both of its biases *total* and said in writing that `EVAL-001` — over the first non-harness work — is the first evaluation with any external validity. That is the claim being tested here, and the composition bias is materially different rather than gone: the five site items are non-harness work, but the fourteen-odd harness and bugfix items they opened are not.

### Two facts settled with the author, so the scorecard does not re-derive them

1. **Scored set = post-baseline only** (above).
2. **The four synthetic fixture directories `EVAL-000` excluded — `rn`, `rn2`, `sep`, `unknown` — are gone because the author removed them.** `H-03` denies every vector an agent has, so only a human could have; the author confirms they did. `GAP-06` and `GAP-07` are therefore scored **resolved**, with that confirmation as their provenance, rather than re-declared as exclusions by this scorecard and every one after it.

## What was built before delegating, and why

`harness-evaluator` has 60 turns and a documented history of spending all of them without producing a file. `TASK 55` measured the remedy on nine slices of equal size: briefed with a pre-written extract and forbidden to open the source, **0 of 3 were cut**; briefed to go read three documents and derive the rest, **1 of 1 was cut at ~100k tokens with no output at all**. So the orchestrator precomputes the corpora rather than naming them.

| Artifact | Covers | Built |
|---|---|---|
| `progress/2026-08-27-12-eval001-trace-index.md` | `evidence/runs/` — 104 trace files, every header/footer/posture and all 40 deny decisions with exact pointers | session 12 |
| `progress/2026-08-27-13-eval001-workitem-extract.md` | `TASKS.md`'s 61 items and 96 `progress/` logs — every `done:` block digested, the `iterations` value verbatim, and the citation graph between items | **this session** |
| `progress/2026-08-27-13-build-workitem-extract.mjs` | the reproducible generator for the above; re-running it reproduces the file byte for byte | **this session** |

Both extracts count and point. Neither interprets, and both say so in their own headers: a scorecard assembled from a document that had already drawn the conclusions would be scoring the extract, not the harness.

**What the extract already establishes, before any judgment is applied:** `K1` has a substrate for the first time. `EVAL-000` reported it `unmeasurable` with raw 0 for two independent reasons — nothing had completed, and no procedure step captured an iteration count. 75 of 96 logs now carry an `iterations` dimension: 47 report `1`, 13 report `2`, and the tail runs 3, 4, 6, 7, 9, 14, 15. It is `self-reported` — read from prose written by the entity being scored — and the scorecard must mark it so, never beside an `observable` figure with equal confidence.

## Delegation

One run to `harness-evaluator` (`opus`, 60 turns, `Read`/`Grep`/`Glob`/`Write`, write scope enforced to `progress/evaluation-results/`), dispatched 2026-08-27.

**Brief hygiene, the thing that most easily kills a delegation here:** `extractWorkItems` scans the *whole* brief text with `/\bTASK[\s-]?(\d+)\b/gi` and treats every hit as an item being delegated against, with no field separating *the item I am working on* from *an item I am mentioning*. The five site items are typed `feature`, so naming even one of them in passing would make `specRequiredFor` demand an approved spec and deny the run before it starts — which has happened twice for real in this repository. **The brief cites `TASK-60` and no other id.** Every other item is named descriptively (*"a content layer"*, *"the two page-template items"*) or reached through a file path.

The brief carries, in order: the fixed frame above; both extracts by path with an explicit instruction not to re-derive them from the sources; the baseline's KPI row verbatim so the comparison columns need no second read; the write-order rule (a structurally complete scorecard by roughly turn 35, deepened after — it holds `Write` and not `Edit`, so every save is a whole-file write, and its two predecessor runs on this same deliverable produced nothing at all); the scoring discipline (`A16`, `P-11`, `unverifiable` available); and the four places the extracts deliberately stop short because deciding them *is* the scoring.

**Verification, before this is treated as finished (`P-18`, `P-11`):** read the delegated run's `run.footer` in the trace — a run that terminates normally writes one and a run that was cut writes none, and the `completed` notification does not distinguish them. Then read the scorecard file itself. If the footer is absent, dispatch a continuation run pointed at the partial file rather than taking the file over: the orchestrator and an agent writing the same file in the same minute is `INC-16`, and the orchestrator is the one actor no write-scope allowlist can reach.

## Verification of the run, before its report was treated as a result

The delegated run wrote `run.footer` at `seq:114` — `COMPLETE / objective_reported` — so it terminated normally rather than being cut, which is the only thing that distinguishes the two and is not visible in the `completed` notification (`P-18`). Its own header reads `permission_mode: unknown` at `seq:1` then observed `auto` at `seq:3`, `enforcement_environment: policy-controlled`. **Zero denials in its trace**, and every path it wrote is inside `progress/evaluation-results/`.

Then the artifact itself was read rather than the summary (`P-11`), and five of its load-bearing claims were checked against primary sources:

| Claim | Checked | Result |
|---|---|---|
| `path-boundary` denies pure reads | `scripts/guards/lib/path-boundary.mjs:82-86, 113-118` | **Confirmed in the source.** `sed`/`perl`/`awk` are flagged by executable name, and every non-flag argument of a mutator is flagged — so `cp <src-in-resources> <dst>` is denied on its source |
| the verbatim denied command | the trace, by `grep` | **Confirmed.** `sed -n '1,200p' resources/site/ui.en.md \| grep -n "article" -A 40` — a pure read, denied under `H-02` |
| `EC-014`'s control shipped while its case file says otherwise | `evidence.mjs:170-183`, `evidence.test.mjs:182`, `TASKS.md`'s register | **Confirmed.** `opaqueFields` is threaded by field name, the red test exists, the item is `DONE`, and the case file still reads `proof: none` / `outcome: Gap` |
| six footer-only orphans post-baseline | the trace index | **Confirmed, and it reconciles:** 7 such files on disk, 1 of them in the baseline directory, 6 post-baseline |
| 23 delegated traces with no footer | the trace index | **24, not 23** — see below |

**Two corrections to the scorecard's figures, recorded rather than smoothed over.** The count of delegated trace files carrying a header and no footer is **24**, all of them post-baseline; the scorecard says 23. The likely explanation is that one of the 24 is the deliberate `budget-probe` run, whose footerlessness is the documented red-path result behind `G-06`'s amendment and is not a defect — a defensible exclusion, but the scorecard does not say it made one, so the register carries both numbers and the reason. Nothing else in the scorecard depends on which figure is right.

## Findings

**The finding reproduced itself on the orchestrator, mid-verification.** Checking the false-denial claim meant reading a trace file: `sed -n '9p' evidence/runs/…/implementer-a1f0eff76cf419cd8.jsonl` was denied by `H-03` as a write to `evidence/`. `H-03` opens with *"Reads are open"*; the guard denied a read. So the defect the evaluator measured on `resources/**` exists identically on `evidence/**`, and it was demonstrated live by the act of auditing it. That second instance is recorded in `TASK 61`, and it is worth more than the fifteen counted ones, because a guard that denies the audit of itself is the shape that hides the next finding.

**What the evaluation is worth, in one line:** it produced a number the harness had never had (`K1`, item-level: 15, 14, 6, 7, 9 on the five items the harness did not author, against a target of ≤2), and it found that 44% of this harness's denial history is its own guard misfiring — the same class of defect `INC-14` produced, in a different guard, unfixed.

**Two things it declined to do, both correct.** It refused to report `K2` as 2 → 0 improvement, because the substrate that produced the baseline's 2 does not exist and 0-observed cannot be distinguished from 0-recorded. And it reported delegated `L` as `unmeasurable` rather than 0, because a zero implies a measurement came back empty.

**One finding against a previous scorecard.** `EVAL-000` reported 5 denied attempts in its scored set; the trace carries 6. The baseline's own `V` is understated by one against the trace it claims to read. Not corrected in place — a past scorecard is a record of what was concluded, and rewriting it would destroy the only evidence that the correction happened (`P-11` cuts both ways). It is recorded here and in `EVAL-001`.

## Gaps filed

Twelve improvement items in the scorecard became **seven** register entries, and both compressions are declared rather than silent (`P-06`, `P-01`):

| filed | covers |
|---|---|
| `TASK 61` | `path-boundary` denies reads that `H-02` and `H-03` permit — 15 of 34 denials, plus the live `evidence/` instance found while verifying |
| `TASK 62` | `L` on the delegated path: instrument it or record honestly that it cannot be (`GAP-03`, third scorecard) |
| `TASK 63` | the paired-predicate assertion reaches every gate step — the eight PASS-while-doing-nothing escapes |
| `TASK 64` | trace fidelity, second pass: four writer defects on one surface, folded on `TASK 12`'s precedent |
| `TASK 65` | `check-evals` cannot detect a stale `proof: none` — `INC-07`'s shape inside the checker built to prevent it |
| `TASK 66` | work-item status transitions, so `K2` has a substrate |
| `TASK 67` | `harness-evaluator`'s 60-turn budget is conditional on precomputed extracts, and the role file does not say so |

**Referenced rather than duplicated:** the omitted-dimension check is `TASK 14`, which gained `EVAL-001`'s harder specimen (a block declaring three dimensions and dropping five, where its existing specimen declares none at all); the CI result is `GAP-12`, carried by `TASK 30`; `EC-013`'s case-folding residual is `TASK 11`.

**Two more the gate run itself opened**, neither of them from the scorecard:

| filed | why |
|---|---|
| `TASK 68` | `check-procedures` requires a `done:` block of every `progress/*.md` and cannot tell a session log from generated tool output. This session's extract is the instance. The three obvious fixes are each wrong for a different reason and all three are recorded in the entry, so the next session does not re-derive them |
| `TASK 69` | an `/about` byline assertion times out on Firefox navigation under the full 513-test load and passes 25 of 25 when its spec runs alone. `TASK 57` closed this class; it recurred. `T-06`: a flake is a finding, and raising the timeout is explicitly not its done |

## The gate

**18 of 20 steps PASS.** Both failures were diagnosed rather than retried, and neither is caused by a file this item changed — no file under `site/` was touched, and the working tree held only `TASKS.md` and additions under `progress/`.

- **`procedures`** — one finding, this session's generated extract carrying no `done:` block. `TASK 68`.
- **`e2e smoke`** — 308 passed, 1 failed, 204 skipped; the failure is a 30s navigation timeout on Firefox for `/about` under full load, and the same spec passes 25 of 25 alone in 59.4s. `TASK 69`.

Worth recording as a change in the other direction: **the `evidence trace` step passes.** Every site item since the content layer closed with it red, declared `partial` each time with the same cause. It is green in this run.

## Done

```yaml
done:
  scope:      { status: passed, evidence: ["the frame was fixed and written down before any scoring, per the procedure's step 1: scored set, posture, baseline-vs-evaluation", "the evaluator wrote only inside progress/evaluation-results/ — every file_path in its trace confirms it", "no artifact under evaluation was edited: the stale EC-014 case file and EVAL-000's understated V are recorded as findings, not fixed in place"] }
  delegation: { status: passed, evidence: ["one harness-evaluator run, run.footer COMPLETE/objective_reported at seq:114 — checked in the trace before the report was read (P-18)", "zero denials in its trace", "~37 of 60 turns, with both corpora precomputed"] }
  evidence:   { status: passed, evidence: ["five load-bearing claims verified against primary sources rather than accepted from the report (P-11)", "two figures corrected: 24 footerless delegated traces rather than 23, with the likely reason named"] }
  loose_ends: { status: passed, evidence: ["twelve improvement items became TASK 61-67, with both compressions declared", "three referenced rather than duplicated: TASK 14, GAP-12/TASK 30, TASK 11", "TASK 14 gained EVAL-001's partial-omission specimen"] }
  docs:       { status: passed, evidence: ["node scripts/guards/gate/check-docs.mjs — PASS, 60 living documents, 235 path references resolved", "node scripts/guards/gate/check-evals.mjs — PASS, 14 cases covering 16 incidents", "node scripts/guards/gate/check-rules-registry.mjs — PASS"] }
  gate:       { status: partial, evidence: ["node scripts/gate.mjs — 18 of 20 steps PASS"], reason: "two steps red, both diagnosed rather than retried, and each now owns a work item. `procedures` fails on this session's generated extract, which carries no done block because it is tool output and not a work log — TASK 68, with the three wrong fixes recorded. `e2e smoke` fails one Firefox navigation under full load (513 tests, 3 engines) and passes 25 of 25 when that spec runs alone — TASK 57's class recurring, filed as TASK 69. Neither is caused by a file this item changed: no file under site/ was touched" }
  tests:      { status: not_applicable, reason: "no production code changed — one read-only diagnostic script, one scorecard written by the evaluator, and register/log prose. The script's own correctness is checked by its reproducibility, recorded below" }
  mutation:   { status: not_applicable, reason: "build-workitem-extract.mjs is a one-off diagnostic, not part of the mutation-covered surface (D3 scopes it to scripts/guards/lib and site/lib)" }
  content:    { status: not_applicable, reason: "harness work — resources/** untouched, and H-02 guarantees it" }
  ci:         { status: not_applicable, reason: "no remote exists yet (TASK 30) — which is itself one of this evaluation's findings, EC-007 scored Gap on exactly that" }
  iterations: { status: passed, evidence: ["1"] }
```

## Files changed

`progress/2026-08-27-13-build-workitem-extract.mjs` — new, read-only generator.
`progress/2026-08-27-13-eval001-workitem-extract.md` — new, the second precomputed extract.
`progress/2026-08-27-13-task60-eval001.md` — this log.
`progress/evaluation-results/EVAL-001-first-non-harness-work.md` — new, **written by `harness-evaluator`**, not by the orchestrator.
`TASKS.md` — `TASK 60` → `DONE`; nine new entries (`TASK 61`–`TASK 69`); `TASK 14` gained `EVAL-001`'s partial-omission specimen.

## Next

The milestone's harness half is closed. Its other half — the author's own judgment of the site — and `TASK 27`'s local and design legs are what remain before `TASK 30`.

**The one thing the next evaluation should carry forward**, in the scorecard's own words: `K1` and `K3` are read from artifacts the scored entity authors, while `V` and `L` are read from the trace, which it does not. The two metrics carrying the verdict are the two least defended against optimism, and no amount of care inside a scorecard fixes that — an implement→verify iteration is not a tool call, so nothing correlates the two today.
