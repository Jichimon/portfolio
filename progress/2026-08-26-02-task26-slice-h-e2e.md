# 2026-08-26 · Session 02 — TASK 26 slice H: e2e for about/experience/404

**Task:** TASK 26 — e2e smoke coverage for about, experience, 404 pages
**Status after this session:** IN PROGRESS

## What was done
(filling in as work proceeds)

## Decisions
(filling in as work proceeds)

## Findings from validating against real state (P-04)
(filling in as work proceeds)

## Done
```yaml
done:
  tests:      { status: passed, evidence: ["npx playwright test about-experience-404.smoke.spec.ts --project=chromium -> 25 pass 0 fail", "full suite across chromium, firefox and webkit -> 303 pass"] }
  tdd:        { status: not_applicable, reason: "an end-to-end tier over pages that already exist; the red path below is what proves it discriminates, not a prior failing state" }
  red_path:   { status: passed, evidence: ["two mechanisms neutered with the build caches cleared -> 2 of 25 fail, naming the locale join and the unwritten-lead omission", "restored -> 25 pass"] }
  scope:      { status: passed, evidence: ["one new file: site/tests/e2e/about-experience-404.smoke.spec.ts", "no existing spec, component, page module or config modified by this slice"] }
  iterations: { status: passed, evidence: ["1"] }
```

**Written by the orchestrator, not by the delegated agent, and the reason is the point of this log.**

The agent was launched with a brief naming three existing spec files to read and instructing it to derive its expectations from the content. It spent **50 tool calls and roughly 100k tokens and produced no spec file at all** — only this log's skeleton. Its final message was that it was about to start looking at the page sources. It never reached its output, because the input was too expensive.

**That is the orchestrator's error, not the agent's.** Every component slice in this item was handed a pre-written extract and forbidden from opening the artboards, and all three of those completed. This slice was handed a reading list. The bounded-reading discipline was applied to the work that produces markup and not to the work that verifies it, and the one that was left unbounded is the one that died — which is the same finding `TASK 12` records from the other side, arriving through a door the orchestrator opened.

The spec was then written directly, from selectors the orchestrator had already read off the built HTML during verification, in a fraction of the budget the delegation consumed.

## Open questions
(filling in as work proceeds)

## Next
(filling in as work proceeds)

## Files changed
- `site/tests/e2e/about-experience-404.smoke.spec.ts` — new e2e smoke spec covering about, experience, and 404 pages.
