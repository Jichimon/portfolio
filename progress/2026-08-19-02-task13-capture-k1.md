# 2026-08-19 · Session 02 — TASK 13: Capture K1

**Task:** TASK 13 — Capture K1
**Status after this session:** `DONE`.

## What was done

Spec-first per `work-item`: wrote and got approval on `docs/specs/SPEC-TASK-13-capture-k1.spec.md`, then delegated one `implementer` slice scoped to five files. It implemented all three behaviors test-first, reported red evidence for each, and flagged one loose end (this session's own earlier log needing an `iterations` line) rather than reaching outside its file scope to fix it. Closed that loose end in Reconcile, confirmed with a full gate run.

## Decisions

- **Reused the existing `doneBlockRequiredFrom` mechanism** for the new `iterationsRequiredFrom` cutoff, rather than a new parser or config shape — same date-threshold-with-written-reason pattern already proven. Rejected: a type-based applicability rule (only require `iterations` on `feature`/`harness`/`bugfix` items) — that's TASK 14's general mechanism, out of scope here, and a narrower one now would need redoing once TASK 14 lands.
- **Cutoff set to 2026-08-19**, the day this spec was approved. Consequence, accepted deliberately: this session's own first log (Part A, TASK 5 item 8 closure) falls on the cutoff and needed its own `iterations: not_applicable` line, added in this session's Reconcile step.
- **`iterations` evidence must be a bare integer, not prose**, so a future evaluator (`EVAL-001`) reads it without interpreting English — mirrors `C-01`'s logic (a number without a checkable shape is not usable as a number).

## Findings from validating against real state (P-04)

- The implementer's report claimed 27/27 tests passing and a specific gate failure shape; both verified directly (`node --test scripts/guards/lib/procedures.test.mjs` → 27 pass 0 fail; `node scripts/gate.mjs` failed at exactly `check-procedures` with exactly the one finding named, before Reconcile). The report matched reality.

## Done

```yaml
done:
  tests:      { status: passed, evidence: ["node --test scripts/guards/lib/procedures.test.mjs", "27 pass 0 fail", "node --test \"scripts/guards/**/*.test.mjs\"", "378 pass 0 fail"] }
  gate:       { status: passed, evidence: ["node scripts/gate.mjs", "exit:0, 13 steps green"] }
  docs:       { status: passed, evidence: ["SPEC-TASK-13-capture-k1.spec.md status: active, approved_version: 1.0", ".claude/skills/work-item/SKILL.md step 7 updated"] }
  ci:         { status: not_applicable, reason: "no remote exists" }
  iterations: { status: passed, evidence: ["1"] }
```

One implement→verify cycle: the implementer's single delegated slice returned complete and correct on first verification — no rejected artifact, no second round.

## Open questions

None outstanding for this item.

## Next

Start TASK 7 (Founding ADRs), decision 1: site stack — first candidate for the dependency-ordered sequence agreed with the human.

## Files changed

`docs/specs/SPEC-TASK-13-capture-k1.spec.md` — new, approved.
`scripts/guards/lib/procedures.mjs` — `validateIterationsRequired`, `validateIterationsEvidence`.
`scripts/guards/lib/procedures.test.mjs` — 7 new tests (20 → 27).
`scripts/guards/gate/check-procedures.mjs` — wires the two new checks, reads `iterationsRequiredFrom`.
`scripts/guards/guards.config.json` — `procedures.iterationsRequiredFrom` + rationale.
`.claude/skills/work-item/SKILL.md` — step 7 (Close) instructs capturing `iterations`.
`progress/2026-08-19-01-*.md` — `iterations: not_applicable` line added (Reconcile).
`TASKS.md` — TASK 13 → `DONE`.
